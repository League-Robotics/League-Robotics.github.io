# Overview

> What this subsystem is and where to start.

<!-- meta: {"order":10,"tags":["intro"],"updated":"2026-08-31"} -->

This is the body of a wiki page. Write normal GitHub-flavored Markdown here — this text
is the source of truth and is read at
`https://github.com/<owner>/<repo>/wiki/overview`.

Note the shape above: an `# H1` title, a `>` one-line blurb, then an optional
`<!-- meta: … -->` comment for machine-readable extras (`order`, `tags`, `updated`).
**No YAML front matter** — a GitHub wiki renders it as visible text at the top of the
page.

Add as many `*.md` files as you like; each becomes one wiki page named after the file.
Link them from `Home.md` and to each other with `[[Page Name]]` or full
`…/wiki/<page>` URLs — relative `.md` links don't resolve, because wiki pages have no
`.md` extension.
