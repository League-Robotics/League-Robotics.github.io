# my-subsystem

> One sentence describing what this subsystem does.

<!-- meta: {"title":"My Subsystem","blurb":"One sentence describing what this subsystem does.","order":100} -->

The `meta` comment above is optional. Its `title` / `blurb` / `order` override whatever
the hub's `subsystems.yml` says for this subsystem, so you can rename yourself without
a PR against the hub. Everything above renders; the comment does not.

`Home.md` is where the hub's card lands, so it is the first thing anyone sees. Keep it
as the index of this wiki.

## Pages

- [Overview](https://github.com/OWNER/REPO/wiki/overview) — what this subsystem is and where to start.
- _One line per page, with its blurb. Replace `OWNER/REPO` throughout._

## Things to remember

- _Decisions, gotchas, and constraints a future agent must not forget._

## Open tasks

- [ ] _Outstanding work, in priority order. Check items off as they land._

> Update this page whenever you add a page or finish a task.
