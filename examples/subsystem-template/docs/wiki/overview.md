---
title: Overview
blurb: What this subsystem is and where to start.
order: 10
# slug: overview        # optional — stable id; defaults to the filename
# tags: [intro]         # optional
---

# Overview

This is the body of a documentation page. Write normal GitHub-flavored Markdown
here — this text is the source of truth and is rendered verbatim on the hub at
`https://league-robotics.github.io/subsystems/<name>/<slug>/`.

Add as many `docs/wiki/*.md` files as you like. Each becomes one page on the hub.
Only the front matter (`title`, `blurb`, …) is special; everything below it is your
documentation.
